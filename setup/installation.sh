#!/bin/bash

# You should download this script, e.g. via wget and run in inside the directory you want to have the application.
# Therefore you should create a directory first, likely inside /var/www/

echo "Downloading repository and extracting it..."
wget 